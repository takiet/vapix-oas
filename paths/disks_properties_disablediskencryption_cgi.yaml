get:
  summary: disablediskencryption
  parameters:
    - name: schemaversion
      in: query
      schema:
        type: integer
      example: "1"
    - name: diskid
      in: query
      schema:
        type: string
      example: SD_DISK
  responses: {}
  tags:
    - Edge Storage
