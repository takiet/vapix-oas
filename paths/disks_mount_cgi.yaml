get:
  summary: mount
  parameters:
    - name: action
      in: query
      schema:
        type: string
      example: mount
    - name: diskid
      in: query
      schema:
        type: string
      example: SD_DISK
  responses: {}
  tags:
    - Edge storage Disk management 1
