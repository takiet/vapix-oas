get:
  summary: setcleanuppolicy
  parameters:
    - name: schemaversion
      in: query
      schema:
        type: integer
      example: '1'
    - name: diskid
      in: query
      schema:
        type: string
      example: SD_DISK
    - name: cleanuppolicy
      in: query
      schema:
        type: string
      example: fifo
  responses: {}
  tags:
    - Edge storage Disk properties 1
