get:
  summary: job
  parameters:
    - name: jobid
      in: query
      schema:
        type: integer
      example: '47'
    - name: diskid
      in: query
      schema:
        type: string
      example: SD_DISK
  responses: {}
  tags:
    - Edge storage Disk management 1
