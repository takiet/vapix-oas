get:
  summary: Activate a virtual input
  parameters:
    - name: schemaversion
      in: query
      schema:
        type: integer
      required: true
      example: 1
    - name: port
      in: query
      schema:
        type: integer
      required: true
      example: 11
    - name: duration
      in: query
      schema:
        type: integer
      required: false
      example: 5
  responses: {}
  tags:
    - I/O
  description: Activate a Virtual Input
