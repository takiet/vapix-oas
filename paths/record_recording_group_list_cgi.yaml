get:
  summary: list
  parameters:
    - name: schemaversion
      in: query
      schema:
        type: integer
      example: "1"
    - name: recordinggroupid
      in: query
      schema:
        type: string
      example: xxx
  responses: {}
  tags:
    - Recording Group
