post:
  summary: getApiList
  requestBody:
    content:
      application/json:
        schema:
          type: object
          properties:
            apiVersion:
              type: string
              enum: ["1.1"]
            method:
              type: string
              enum: ["getApiList"]
  responses: {}
  tags:
    - API Discovery 1
  description: This is the entry point where the rest of the APIs are being discovered
