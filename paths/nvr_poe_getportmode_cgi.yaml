get:
  summary: getPortMode
  parameters:
    - name: port
      in: query
      schema:
        type: integer
      example: "1"
    - name: schemaversion
      in: query
      schema:
        type: integer
      example: "1"
  responses: {}
  tags:
    - NVR PoE
