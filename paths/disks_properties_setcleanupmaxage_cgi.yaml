get:
  summary: setcleanupmaxage
  parameters:
    - name: schemaversion
      in: query
      schema:
        type: integer
      example: '1'
    - name: diskid
      in: query
      schema:
        type: string
      example: SD_DISK
    - name: maxage
      in: query
      schema:
        type: integer
      example: '7'
  responses: {}
  tags:
    - Edge storage Disk properties 1
