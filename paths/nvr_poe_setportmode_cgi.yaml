get:
  summary: setPortMode
  parameters:
    - name: port
      in: query
      schema:
        type: integer
      example: '1'
    - name: enabled
      in: query
      schema:
        type: string
      example: 'yes'
    - name: schemaversion
      in: query
      schema:
        type: integer
      example: '1'
  responses: {}
  tags:
    - NVR PoE 1
